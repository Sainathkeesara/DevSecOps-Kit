# README Layout audit: remaining root folders confirmed documented

## Purpose

This audit note records the finding for gen-006: verify whether README Layout documents the root folders `assets/`, `cosign/`, `falco/`, and `terrascan/`.

## Findings

All four folders are already documented:

- **`assets/`** — Static images and diagrams used by guides and project docs (added by gen-009).
- **`cosign/`** — Cosign notes, scripts, and snippets (was already listed).
- **`falco/`** — Falco configs and notes (was already listed).
- **`terrascan/** — Terrascan primers, notes, and IaC scan snippets (added by gen-009).

No further README Layout changes are required for these folders. The work described in gen-006 is already complete.