# last_verified: 2026-09-19 · grafana n/a
# My first Grafana data source — I just wanted one Prometheus backend wired up
# so I could try a panel query. Haven't tried alerting or extra sources yet.
apiVersion: 1

datasources:
  - name: my-first-prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: true
